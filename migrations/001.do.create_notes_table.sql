CREATE TABLE IF NOT EXISTS notes_table (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    content TEXT,
    folderNum INTEGER REFERENCES folders_table(id) NOT NULL,
    modified TIMESTAMP NOT NULL DEFAULT now()
);